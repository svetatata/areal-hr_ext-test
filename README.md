## Инструментарий
- OS: CachyOS
- IDE: VS Code + расширения (может быть изменено) 
- PostgreSQL будет установлен через Docker

## Основные команды Git
<pre>git status
git diff [--staged]
git add [., file.txt]
git restore [., file.txt]
git commit -m "текст" 
git push
</pre>
